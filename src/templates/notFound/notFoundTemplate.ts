import template from 'templates/notFound/notFoundTemplate.html';
import Template from 'model/Template';

const notFoundTemplate = new Template('notFound', template);

export default notFoundTemplate;
